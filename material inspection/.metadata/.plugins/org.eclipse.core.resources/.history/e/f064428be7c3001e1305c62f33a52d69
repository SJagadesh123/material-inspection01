package com.zettamine.mi.controller;

import java.util.List;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;

import com.zettamine.mi.entities.Material;

@Controller
@RequestMapping("/material-inspection/material")
public class MaterialController {

	
	
	@GetMapping("/new")
	public String getNewMaterial(Model model)
	{
		model.addAttribute("material", new Material());
		
		return "add-material";
	}
	
	@PostMapping({"/add-material","/edit/add-material"})
	public String addPlant(Material material,Model model)
	{
		System.out.println(material);
		
		//plantService.save(material);
		
		return "material";
	}
	
	@GetMapping("/search")
	public String getShowAll(Model model)
	{
		
		//List<Plant> allPlants = plantService.getAll();
		
		//model.addAttribute("allPlants", allPlants);
		
		return "show-plant";
	}
}
